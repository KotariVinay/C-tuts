#include<stdio.h>
int main()
{
   int num;
   scanf("%d",&num);
   if(num%2==0)
   {
       printf("the given number is even number");
    }else
    {
	printf("the given number is odd number");
    }
}
