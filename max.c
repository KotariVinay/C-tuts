/* Write a program to find the max of two numbers */
#include<stdio.h>
int main()
{
       int num1,num2;
       printf("Enter the value of num1 :\n");
       printf("enter the value of num2 :\n");
       scanf("%d \n%d",&num1,&num2);
       if(num1>num2)
       {
            printf("num1 is graeter than num2\n");
       }else
       {
	   printf("num2 is greater than num1\n");
       }
}
